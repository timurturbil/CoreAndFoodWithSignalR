

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<ul>
    @foreach (var item in ViewBag.connectedUserIds)
    {
        <li>@item.Key @item.Value</li>
        <a href="/Server/SetSelectedUser/@item.Value">Click</a>
    }
</ul>

